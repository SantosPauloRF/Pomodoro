{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Janela principal e overlays de todos os monitores",
  "windows": ["main", "overlay-*"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default"
  ]
}
